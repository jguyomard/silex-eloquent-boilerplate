{% extends 'layout/default.html.twig' %}

{% block content %}

    <div class="row">
        <div class="columns">
            <h2>{{ article.article_title }}</h2>
            <p>{{ article.article_description }}</p>
        </div>
    </div>

{% endblock %}
